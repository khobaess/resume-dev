server:
  port: 8080
  servlet:
    context-path: /

spring:
  application:
    name: resume-dev
    
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:dev}
    
  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:postgres}
    username: ${DB_USERNAME:postgres}
    password: ${DB_PASSWORD:postgres}
    driver-class-name: org.postgresql.Driver
    hikari:
      maximum-pool-size: 20
      minimum-idle: 5
      connection-timeout: 30000
      idle-timeout: 600000
      max-lifetime: 1800000
    
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
        jdbc:
          batch_size: 20
        order_inserts: true
        order_updates: true
    open-in-view: false
    
  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true
    
  jackson:
    serialization:
      write-dates-as-timestamps: false
    time-zone: Europe/Moscow
    default-property-inclusion: non_null
    
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

app:
  openapi:
    dev-url: ${DEV_URL:http://localhost:8080}
    #prod-url: ${PROD_URL:https://api.achievements.vk.com}
    
  vk:
    api-url: ${VK_API_URL:https://api.vk.com/method}
    api-version: ${API_VERSION:5.131}
    app-id: ${VK_APP_ID:53924188}
    secret-key: ${VK_SECRET_KEY:iV3DLqsVrNxFjsH0s1Ne}
    service-token: ${VK_SERVICE_TOKEN:5d6b2e6b5d6b2e6b5d6b2e6bfd5e5dff3755d6b5d6b2e6b35aa5ccc190b0002f9bf8fd6}

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics,prometheus
  endpoint:
    health:
      show-details: when-authorized
  prometheus:
    metrics:
      export:
        enabled: true

---
spring:
  config:
    activate:
      on-profile: dev
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true
logging:
  level:
    com.vk.achievements: DEBUG
    org.hibernate.SQL: DEBUG

---
spring:
  config:
    activate:
      on-profile: prod
  jpa:
    show-sql: false
  datasource:
    hikari:
      maximum-pool-size: 50
logging:
  level:
    com.vk.achievements: INFO
    org.hibernate.SQL: WARN
